# BybitScanner — клин как локальная формация: внешний опыт и план реализации

Дата анализа: 2026-09-20. Статус: RESEARCH / DESIGN; код и торговые полномочия НЕ одобрены этим документом.
Ветка документа: черновой PR #156. Основной контур, уже открытые Robot-кандидаты, PAPER DB, процессы ПК/VPS не изменять.

## 1. Задача и реальные примеры пользователя

Проблема — не в длине отображаемого графика и не в косметическом переносе START.
Текущий детектор способен объединять опорные точки из разных рыночных эпизодов, подбирать
длинные линии, выставлять 95–100 баллов и показывать фигуру, которой пользователь
не видит. Из-за этого нельзя начинать G2b/G3/G3P с выбранного детектором START.

Примеры из пользовательских сравнений Scanner PNG и вручную размеченного TradingView
(в этой задаче изображения видны в разговоре; точные source bar ID/снимки в репозитории
**не подтверждены**; нельзя восстанавливать их по пикселям):

| Пара, TF | Пользовательская разметка | Что проверяем |
| --- | --- | --- |
| SOLUSDT, 5m | Отрицательный пример: у сканера первые HIGH/LOW анкеры на индексах 19 и 124 (105 баров = 8 ч 45 мин), локального клина здесь нет. | Почему кандидат переживает фильтр и получает CANONICAL/95; не исправлять более длинной историей. |
| 0GUSDT, 5m | Отрицательный пример: верхняя и нижняя линии не образуют убедительной локальной формации. | Отбор локального эпизода и фактическая оболочка цены, а не качество картинки. |
| ALTUSDT, 5m | Положительная пользовательская цель: нисходящий клин *замедления* после резкого падения; правильная пара границ локальнее сканерной. | Для этой гипотезы START — переходный LOW, затем соседний разворот HIGH; сравнить сохранённые pivots. |
| CRCLUSDT, 5m | Положительная пользовательская цель: нисходящий клин *замедления* после резкого падения. Пологая нижняя линия идёт от переходного LOW, верхняя — от ближайшего последующего HIGH. | LOW → HIGH начало локального эпизода; сканер взял неверную позднюю нижнюю границу. |
| CRVUSDT, 5m | Положительная пользовательская цель: восходящий *коррекционный* клин после резкого падения. Нижняя линия начинается с терминального LOW, верхняя — с последующего локального HIGH. | Тоже LOW → HIGH; сама последовательность не отличает коррекцию от замедления. |
| DATAUSDT, 5m | Ещё один пользовательский сравнительный пример: исходный Scanner PNG растягивает нисходящие линии назад через более раннюю консолидацию и сильный рост; на пользовательском TradingView рисунке обе линии начинаются локальнее, после последующего резкого падения. Гипотеза: FALLING_DECELERATION_AFTER_DOWN, **ещё не подтверждена source-time пивотами**. | Отсечь ранний episode/anchor drag и восстановить локальные LOW → соседний HIGH и поздние касания, если они были подтверждены к исходному сигналу. Не переносить готовые линии с картинки по пикселям. |

Эти метки описывают ВИДЕНИЕ пользователя, а не уже подтверждённые машиной anchor
indices/пройденный прогноз. До выбора порогов сопоставить каждый исходный
сигнал-time snapshot с source OHLC, raw/filtered pivots и закрытыми свечами.
Если исходный снимок не сохранился, отдельно пометить MISSING_REPLAY_DATA; новую
котировку нельзя подменять историческим сигналом. Ручной чертёж не доказывает,
что оба его крайних pivot уже были подтверждены в момент старого сигнала.

Ключевая модель:
- Геометрия: FALLING / RISING — знак наклона обеих границ и проверенное схождение.
- Контекст: движение ДО локальной формации (UP / DOWN / UNKNOWN).
- Подтип: CORRECTION / DECELERATION / UNKNOWN — отдельная гипотеза, подтверждаемая
  предварительным импульсом и изменением динамики внутри формации.
- Исторический episode START: переходный экстремум (может совпасть с первым
  анкером только одной границы), не max/min line-anchor и не начало заливки.
- Первая пара HIGH/LOW: близкие значимые противоположные swing points одной
  формации, но **без глобально фиксированного порядка HIGH→LOW или LOW→HIGH**.
  Для FALLING_CORRECTION_AFTER_UP START может быть HIGH; для показанного
  FALLING_DECELERATION_AFTER_DOWN — LOW; для RISING_CORRECTION_AFTER_DOWN — LOW.
  Правило для RISING_DECELERATION_AFTER_UP ещё требует пользовательской разметки.
  Эти наблюдения НЕ позволяют угадать подтип только по порядку анкеров.

## 2. Что конкретно делают зрелые проекты

Все ссылки — первичные официальные описания поведения, а не доказательство
точных алгоритмов закрытых коммерческих реализаций; внутренний код TradingView
Auto Chart Patterns неизвестен. ADAPT означает взять идею и определить свою
реализацию, DEFER — только как резерв, REJECT — не переносить.

### 2.1 TradingView Zigzag — одна ХРОНОЛОГИЧЕСКАЯ последовательность swing highs/lows

Официальный Zigzag соединяет значимые подтверждённые pivot TOP/BOTTOM
попеременно и использует minimum reversal percentage, а не строит два
независимых хронологических списка границ:
https://www.tradingview.com/support/solutions/43000591664-zigzag-indicator/

Решение ADAPT: сохранить в BybitScanner raw pivot-high/low, объединить по
source candle/time, отдельно сформировать **одну чередующуюся структурную
последовательность** с provenance. Несколько same-side локальных максимумов
до следующего LOW — одна конкурирующая swing-нога, более высокий релевантный
экстремум может заменить предварительный HIGH, но уже подтверждённые
исторические решения задним числом не переписываются. Не заставлять
filter_pivots(highs) и filter_pivots(lows) по отдельности определять, какая
пара открывает один эпизод. Zigzag-направление и чувствительность сами по
себе не выбирают готовый клин, контекст и вход робота.

### 2.2 TradingView Auto Trendlines — multi-scale swings и границы протяжённости

Официальная реализация анализирует отдельные малые и крупные Zigzag-уровни,
требует значимого разворота относительно ATR, строит линию по реальным
опорным точкам, дополнительно измеряет касания, фильтрует пересечения линий
и ограничивает необоснованное продолжение линии:
https://www.tradingview.com/support/solutions/43000741165-auto-trendlines/

Решение ADAPT: отделить pivot detection, scale/significance, forming episode,
anchored-line fit и контроль времени существования линии. Считать
ATR-нормированную величину swing, длительность и относительную значимость
пивота; проверить, что обе границы описывают **один общий активный локальный
эпизод**, а не что две независимые длинные линии визуально пересекаются.
Исторический анкер до эпизода не становится валидным лишь благодаря
поздним касаниям. НЕ копировать опубликованные настройки 5/5, 25/25,
2×ATR/5×ATR и лимит длины проекции: они не проверены на нашем 1m/5m
USDT-perpetual потоке.

### 2.3 QuantConnect LEAN Zig Zag — потоковая смена swing-состояния

Официальный индикатор чередует high/low значимые развороты, использует
reversal sensitivity и minimum trend length; поддерживает последовательное
обновление по новым барам:
https://www.quantconnect.com/docs/v2/writing-algorithms/indicators/supported-indicators/zig-zag

Решение ADAPT: маленький state machine для текущего HIGH/LOW и их
confirmation/availability. Для сравнения — дополнительный Zigzag-like
*research* extractor на том же закрытом OHLC. НЕ добавлять библиотеку LEAN,
не заменять без сравнения действующий find_pivots и не считать подвижный
последний swing уже подтверждённым anchor.

### 2.4 StockCharts ChartSchool — фигура из серии reaction highs/lows и prior trend

Официальный разбор Falling Wedge требует по крайней мере 2 reaction highs
и 2 reaction lows, сходящихся линий и различает reversal after DOWN и
continuation after UP. У Rising Wedge support/resistance также строятся
по последовательности reaction lows/highs:
https://chartschool.stockcharts.com/table-of-contents/chart-analysis/chart-patterns/falling-wedge
https://chartschool.stockcharts.com/table-of-contents/chart-analysis/chart-patterns/rising-wedge

Решение ADAPT: проверять *серии внутри одной формации* и предшествующий
импульс отдельно. Четыре честно подтверждённых локальных поворота (2 HIGH,
2 LOW) — минимальная предварительная геометрическая гипотеза, не зрелый
паттерн и не торговая команда. Для G3 измерять разницу импульса и
поступательной скорости/глубины последующих local swings, а не выводить
«замедление» лишь из падающего наклона. Долгие горизонты примеров акций
не переводить в произвольные пороги для крипто 5m.

### 2.5 TradingView Auto Chart Patterns — In Progress и фактическая оболочка

В официальном Falling Wedge строятся линии из pivot endpoints, есть
проверка нежелательных пересечений линии с закрытиями внутри formation
и отдельный In Progress для формирующихся паттернов; последние точки
могут быть неподтверждёнными и помечены пунктиром:
https://www.tradingview.com/support/solutions/43000697938-chart-pattern-falling-wedge/
https://www.tradingview.com/support/solutions/43000653219-chart-pattern-rising-wedge/

Решение ADAPT: проверка фактических HIGH/LOW, CLOSE и реакции пивотов
на ВЕСЬ предполагаемый общий эпизод, чёткое разделение OBSERVED и
PROJECTED границ, PROVISIONAL / CONFIRMED / INVALIDATED. Нельзя оценивать
только от позднего common_start, скрыв старые серьёзные пробои.
Для первого G3P оставляем 4 только ПОДТВЕРЖДЁННЫХ пивота: копировать
TradingView-режим с недозревшими последними точками в Robot недопустимо.
«Подтверждена геометрия» не равняется «подтверждён пробой» или выгодная сделка.

### 2.6 SciPy — prominence, distance и краткий локальный горизонт

Official find_peaks допускает фильтры prominence, horizontal distance
и width, peak_prominences может быть ограничен окном wlen:
https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.find_peaks.html
https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.peak_prominences.html

Решение ADAPT *понятия* относительной prominence, расстояния между
соседними structural turns и bounded lookback, включая поиск LOW через
минусы/отдельный low-series. Сравнить на нашем raw/filtered pivot потоке:
может ли локальный переходный LOW исчезать из независимой фильтрации.
НЕ добавлять SciPy как новую обязательную зависимость сейчас;
не выводить поворот по данным после as_of. Порог prominence — параметр
исследования, пока не выбран.

### 2.7 TradingView Repainting + Freqtrade — никаких исторических «сверхзнаний»

Pivot с left/right подтверждается лишь по окончании right последующих
свечей; отображение его на прошедшем баре не делает его известным тогда:
https://www.tradingview.com/pine-script-docs/concepts/repainting/

Freqtrade проверяет lookahead различиями между полным и усечённым расчётом
и отдельно проверяет чувствительность к объёму стартовой истории:
https://docs.freqtrade.io/en/latest/lookahead-analysis/
https://docs.freqtrade.io/en/latest/recursive-analysis/

Решение ADAPT: модель pivot_event_bar + confirmed_at_bar + observed_as_of,
только ЗАКРЫТЫЕ source TF свечи; если i + right > as_of_closed, такого
подтверждённого пивота нет даже в «pending» списке, полученном из будущего.
Тесты сравнивают полную будущую OHLC/pivot последовательность и её
исторический префикс при одном as_of. Повторный анализ с 1000 свечами
может изменить сам набор кандидатных опор: нельзя использовать его
для «проверки» исходной фигуры с 200 свечами без frozen episode mapping.
G2a уже реализовал этот принцип в отдельном чистом исследовательском
helper, не расширять его без необходимости.

## 3. Где дефект в текущем BybitScanner (repo PR #156, review 2026-09-20)

| Существующий узел | Что фактически делает | Риск на наших примерах |
| --- | --- | --- |
| pivots.py::find_pivots / filter_pivots | Считает HIGH и LOW отдельно; same-side price-change pruning отдельно для каждого списка. | «Следующий LOW» в фильтрованном списке не обязательно соседний значимый разворот после HIGH; пропадает переходный экстремум. |
| geometry/candidate.py::build_candidate_lines | Перебирает пары пивотов ОДНОЙ стороны, требует min_line_span=30 между точками той же границы, потом подтверждения. | Разумный 30-bar span одной границы не обосновывает 30/100 бар между ПЕРВЫМИ верхним и нижним анкером; линия может «прилипнуть» к далёкому свингу. |
| geometry/engine.py::analyze_geometry | Декартово произведение upper × lower; отбирает геометрии с validation.valid, затем предпочитает CANONICAL всем EXPLORATORY, внутри режима выбирает rank_geometry. | Качество линии и приоритет режима могут скрыть более локальную пользовательскую пару; сначала нужна принадлежность одному эпизоду, потом ranking. |
| geometry/pair_metrics.py::calculate_pair_metrics | common_start=max(upper_anchor,lower_anchor), anchor_distance/balance; для FALLING каноническая последовательность HIGH→следующий filtered LOW, для RISING LOW→следующий filtered HIGH. | Далекие «соседи» фильтрованного списка могут считаться корректными; LOW-first falling slowdown по CRCL/ALT может остаться EXPLORATORY. Приоритет CANONICAL делает это существенным, но **не означает**, что модель всегда отбрасывает LOW-first. |
| geometry/evaluation.py::evaluate_candidate_pair / geometry/envelope_metrics.py | start_index=min(first line anchors), pre-pattern endpoint direction считается от этого индекса; часть проверок привязана к common_start. | Исторический переходный START смешан с опорой линии; проверка позднего участка не доказывает целостность раннего участка. |
| chart_clean.py | Метка START визуально приурочена к началу общей заливки; G1a расширяет ОКНО отображения. | Улучшенный график не исправляет ошибочно подобранную пару; подпись не обязана совпасть с historical episode START. |
| geometry/pre_pattern.py + geometry/start_diagnostics.py | G2a/G2b0: read-only evidence/диагностика до раннего анкера; в production геометрию не меняют. | Полезны только ПОСЛЕ допуска локального эпизода; неправильную линию и пропущенный между анкерами переход сами не чинят. |

Не называем причиной SOL/ALT/CRCL/CRV конкретную строку до чтения их
сохранённых source snapshots. Картинки показывают симптом; raw pivot
trace должен подтвердить механизм. Значения 95/100 здесь не
верификация валидной формации и не признак статистического преимущества.

## 4. Варианты решения: стоимость, выгоды, отложенные альтернативы

**A — REUSE FIRST / минимальная локальная замена (первый опыт):**
сохраняем действующий find_pivots, дополнительно берём raw confirmed
HIGH/LOW (до независимого same-side pruning) в небольшой кандидатной
области, строим один chronological alternating swing ledger, локальные
episode proposals с обеими порядковыми гипотезами. Только внутри
эпизода строим/проверяем пары линий, используем текущие line-fit,
containment и scoring. Делаем сначала read-only shadow на snapshots,
затем маленький wedge-only guard. Плюсы: мало новых модулей,
переиспользование зрелых функций, минимальный runtime impact.
Риск: текущие 3/3 или 0.3% raw pivots могут пропускать значимый
переходный экстремум; его нужно измерить, не выдумывать.

**B — multi-scale Zigzag / prominence (резерв после измерения A):**
для нужных локальных участков извлечь альтернативный чередующийся
swing ledger с ATR-relative reversal/prominence и длительностью.
Сравнивать один и тот же historical as_of с A. Плюс: меньше
микрошума и пропусков из independent same-side фильтра.
Цена: дополнительная state/provenance модель, калибровка на 1m/5m,
риск задержать подтверждение и повысить нагрузку на 771 символ.
Не внедрять одновременно с A и не переключать production
пивотную систему без отрицательных/положительных контролей.

**C — neural network, картинка-шаблон, новая торговая библиотека
или полный переписанный Geometry Engine: DEFER/REJECT сейчас.**
Из рисунка нельзя доказать, какие pivot/confirmed_at были доступны
в момент сигнала; ML требует размеченной выборки и отдельного
воспроизводимого датасета. Не заменяет интерпретируемые
эпизоды, fail-closed и текущие freeze/Robot boundaries.

## 5. Готовый план: ОДИН зависимый микрослайс за раз

**Текущую уточнённую очередность S2b→(условный S2c)→S3 и reuse-first ограничения см. в §5A; S0–S2a ниже — исторический исходный план, не повторять выполненные локальные опыты.**

### S0. Контрольные примеры и точная локальная диагностика (read-only)

На ПК в изолированном scanner-g2a берём **существующие неизменные**
research_scanner/snapshots для ALT, CRCL, CRV, DATA, SOL и 0G, если они
действительно присутствуют. Проверяем, что user annotation и оригинальный
source snapshot относятся к одной паре/TF и сопоставимому времени,
иначе помечаем MISSING_ALIGNMENT. Считаем raw и текущие filtered pivots,
их event/confirmation times, соседей, локальную swing amplitude
(%, ATR units), интервалы в source bars, расстояния до anchors,
действительные touches/violations и какой gate дал CANONICAL.
Получаем маленькую таблицу причин на каждый кейс,
не подгоняя линии и не запуская 771-symbol скан заново.
Сохраняем исходный frozen snapshot и user drawing отдельно.

**Выход:** фактическое объяснение наблюдаемых ошибочных и пользовательски
размеченных локальных формаций (включая DATA) и контрольной последовательности; если сырья недостаточно,
останавливаем утверждения о причинах. Исследовательский сканер,
listener и Robot не перезапускать ради S0.

### S1. Чистая episode/swing диагностика с двумя начальными порядками

Подвести raw confirmed points к единому ledger, аккуратно
разрулить coincident HIGH/LOW (AMBIGUOUS), несколько same-side экстремумов
до противоположного swing, отсутствующий opposing pivot,
left-history truncation и gaps. Для каждой bounded локальной
последовательности сохранять source indices/time, first LOW/HIGH,
вторую и следующую пару swings, age, as_of и причины отказа.
Поддерживать HIGH→LOW и LOW→HIGH вне зависимости от ориентации
клина. Первоначальный episode proposal независим от final subtype
(иначе circular inference). Результат изолирован от действующего
winner, уведомлений и Robot. Raw и filtered не переписывать,
вместо этого вернуть строго ограниченную альтернативу из raw.
Ограничивать по числу последних confirmed turns/времени до
попыток Cartesian product; без новой сети на каждого кандидата.

**Выход:** детерминированные положительные/отрицательные случаи,
1m/5m, входные пустые/неполные данные, invalidation reasons.
Никакой придуманной фиксированной границы «N бар» на этот шаг.

**Дополнение для S1b (только измерение):** для уже доступных локальных линий
собрать отдельно нарушения «строгой» границы: у FALLING — верхняя,
у RISING — нижняя. Если локальная линия ещё не строится, пометить
NOT_EVALUABLE, не подставлять старую дальнюю линию. Отдельно фиксировать
выходы тела и теней, число/долю баров с выступающими тенями, их серии и
глубину относительно ATR и локальной ширины формации. Это диагностика
на сохранённых закрытых свечах, НЕ новый gate и НЕ настройка порогов.

### S2. Когерентность линии в одном эпизоде — сначала shadow

Для каждой episode гипотезы взять 2 подтверждённых HIGH и
2 подтверждённых LOW (где доступны), построить обе границы
переиспользованным fit_anchor_trendline, исключить пары линий,
чьи первые опоры не принадлежат одному локальному episode.
Проверки:
- opening opposite pivots соседние **структурно**, а не просто
  соседи независимо отфильтрованного списка; есть измеренный
  следующий разворот и source-time window;
- обе линии ограничивают **те же** reaction swings; проверить
  знаки наклонов, порядок границ, положительную width на
  observed span, истинное сужение и apex относительно observed
  end (не принимать пересечения/расширение);
- общий эпизод содержит достаточно реальных подтверждений
  каждой границы и не имеет серьёзных неподтверждённых ценой
  пересечений/выходов за envelope в его ранней и поздней части;
  текущая tolerance в % от цены не должна легализовать
  нарушение, сравнимое с целой шириной узкого клина;
- **Строгая граница (новое требование пользователя):** для FALLING
  это верхнее сопротивление, для RISING — нижняя поддержка. На всех
  закрытых свечах, относящихся к наблюдаемой формации ДО отдельного
  подтверждённого breakout, тело свечи не может пересечь строгую
  границу: для FALLING `max(open, close) <= upper(t)`, для RISING
  `min(open, close) >= lower(t)` (учитывать только обоснованную
  точность котировки/тик, не текущую процентную tolerance как
  разрешение пробоя тела). Разрешаются лишь небольшие ИЗОЛИРОВАННЫЕ
  проколы наружной тенью (`high > upper(t)` у FALLING,
  `low < lower(t)` у RISING); повторяющиеся/серийные либо суммарно
  существенные выступы теней за строгую границу делают геометрию
  нецелостной даже при небольшом отклонении каждой отдельной свечи.
  В shadow измерить число, долю, непрерывные серии, максимальную и
  накопленную глубину проколов в цене, ATR и долях локальной ширины
  клина; допустимые границы определить по положительным/отрицательным
  сохранённым примерам отдельно для 1m/5m, не назначая произвольный
  процент, количество или допуск. Оценивать весь выбранный локальный
  эпизод, включая участок до более позднего из первых анкеров; выход
  после подтверждённого breakout относится к событию пробоя, а не к
  внутренней целостности завершившейся формации. Нельзя скрывать
  прежние нарушения переносом START или поздним common_start.
- первый анкер не берётся из отдельного более раннего
  импульса ради улучшения fitted error; слишком поздняя
  пара не скрывает настоящий episode START;
- смена локального эпизода, breakdown, missing OHLC и
  unconfirmed terminal возвращают INVALID/UNKNOWN, не 95/100.

**Важно:** relative gap/ATR/episode-duration threshold — измеряемые
параметры, а не магическое «не более X минут»; согласовать
только после сравнения позитивных ALT/CRCL/CRV и отрицательных
SOL/0G на одном TF. Калибровать 1m отдельно от 5m.
Shadow выдаёт диагностику рядом с original winner, не
переписывает старые сигналы и не публикует его результат в Telegram.

### S3. Узкий fail-closed gate и пользовательская проверка локально

Когда S2 восстановит размеченные примеры и отсечёт явно
нецелостные пары, подключить **только для wedge** проверку
coherent_episode до rank_geometry/выбора CANONICAL, не
изменяя отдельно Triangle, STOP/TAKE или Robot. Если не
нашёлся подтверждённый coherent wedge, результат «клин не
определён», **не** подбирать дальние якоря как fallback.
CANONICAL/EXPLORATORY приоритет применять только среди
валидных локальных пар; не заставлять LOW-first FALLING
автоматически быть EXPLORATORY лишь из-за порядка, но и
не маркировать DECELERATION без доказанного импульса.

Сверить прогон **только сохранённых** кейсов, затем
провести отдельную локальную наблюдательную оценку
по новым Telegram сигналам. Новые фотокарточки должны
содержать реальную ту же геометрию/исходную версию.
Измерить число валидных фигур, false positive / missing
coherent wedge на размеченных примерах, долю UNKNOWN
и 1m/5m, длительность одного символа и полных
771-symbol проходов без запрещённого full-suite теста.
Без автоматических торговых кандидатов из экспериментального
результата; не менять уже frozen/approved Robot snapshots.

### S4. START и тип клина на ДОПУЩЕННОМ эпизоде

После S3 задействовать G2a/G2b0 только на локально
когерентной структуре. Historical START выбрать совместно
с подтверждённым предыдущим импульсом и первым episode
turn, а не отрисовкой заливки или min/max line anchor.
Отдельный G3 подтверждает два типа по каждой ориентации,
UNKNOWN для неразрешённых случаев:
- FALLING_CORRECTION_AFTER_UP → HIGH terminal UP impulse;
- FALLING_DECELERATION_AFTER_DOWN → first transition LOW;
- RISING_CORRECTION_AFTER_DOWN → LOW terminal DOWN impulse;
- RISING_DECELERATION_AFTER_UP → mirrored HIGH **пока гипотеза,
  требует пользовательского примера**.
Проверять для slowdown изменение скорости и глубины
successive swings; не путать с одной лишь направленностью
внешнего импульса. В UI показывать «Тип клина: Коррекционный»,
«Тип клина: Замедление» только на валидированном immutable
signal-time context; иначе «Тип клина: не определён».
G1a window/Telegram format — отдельная presentation задача.
Проверить consistency Scanner text/PNG и Robot historical
chart/caption без нового расчёта subtype на поздних свечах.

### S5. Только после S4 — G3P 4 точки, ручное «Освежить», G3C

Строить provisional 4-point wedge из **того же coherent
episode ledger** и четырёх действительно confirmed pivots;
проекцию рисовать как прогноз, отдельные state/revision.
Кнопка Telegram refresh читает именно эту immutable
formation identity и никогда не меняет старый frozen snapshot.
Внутриклиновую PAPER-торговлю исследовать отдельно:
потребуются подтверждённые параметры входа/риска и
владелец, PAPER tests и новое явное разрешение пользователя.
Ни один research tag/кнопка/score не разрешает реальный
Robot order или LIVE; если пользователь хочет вернуть
«🤖 Робот» на исследовательскую карточку, отдельно
согласовать, какие именно сигналы и snapshot eligibility
переходят в штатный admission path.

## 5A. Ускоренный план после S0–S2a: reuse-first (коррекция 2026-09-20)

**Статус:** план последующих исследований, НЕ результат внедрения и НЕ разрешение на изменение торговых полномочий. Этот подраздел уточняет очередность и критерии S2–S3 выше; прошлые решения и уже выполненные этапы не переписываются задним числом. Источник фактов о локальных опытах — отчёты Claude Code и git-ignored diagnostics в `scanner-g2a`, не опубликованные в GitHub. Перед использованием повторно проверить локальные manifest/as_of и точную версию кода; не утверждать, что эти файлы доступны в PR.

### 5A.1. Что уже измерено; где доказательства ограничены

- S0: для ALT/CRCL/CRV/DATA и двух 0G исходная геометрия воспроизведена на сохранённых свечах; SOL исходного `candles.csv`/`analysis.json` не имеет (`MISSING_REPLAY_DATA`). Пользовательские TradingView-разметки, кроме связи нажатия CRCL с Telegram message_id, не сопоставлены однозначно с исходными снимками (`MISSING_ALIGNMENT`).
- S1a: ledger строится только по закрытым на `as_of` свечам, различает EVENT и CONFIRMED_AT и не разрешает HIGH/LOW одной свечи в произвольном порядке. Исходная пара ALT использовала HIGH 196, не подтверждённый на исходном `as_of=198` (ему нужна свеча 199).
- S1b: read-only перепись 395 сохранённых 5m-сигналов/355 source-time последовательностей: из 1283 оцениваемых границ 386 строятся из filtered (A), 673 — только из raw (B), 224 не строятся даже из raw при действующих условиях (C); 5047 не оценены (D), включая правое усечение/неоднозначность. Выборка условна на уже выданные сигналы, повторы зависимы; 1m здесь не изучен. Из 1059 выразимых границ 888 используют подтверждения за пределами предложенного эпизода. Статистика не измеряет прибыльность и не санкционирует смягчение порогов.
- S2a на сохранённых 5m-контролях: CRCL `U119→195 / L114→185` — исследовательская сходящаяся пара с чистой строгой верхней границей на доступном участке; вариант от L123 структурную проверку не прошёл. CRV — варианты с чистым заякоренным участком, но выходами после последнего анкера; завершение `UNKNOWN`. ALT от L117 не даёт нисходящей пары на доступном `as_of`, варианты от L125 расширяются. DATA `U80→146 / L85→135` геометрически сходится, но U80 не имеет дополнительного подтверждения сверх двух анкеров (`UNKNOWN`). У 0G исследуемые пары не прошли, coincident HIGH/LOW остаётся `AMBIGUOUS`. Это результаты исследования выбранных альтернатив, не доказательство пригодности пользовательской разметки или production winner.

### 5A.2. Внешние идеи — взять минимальный алгоритмический принцип, не чужой движок

| Источник | ADAPT для BybitScanner | Не переносить |
| --- | --- | --- |
| TradingView ZigZag, LEAN Zig Zag | Хронологический confirmed swing-ledger; различие EVENT/CONFIRMED_AT и последовательное обновление без переписывания прошлого | Полная замена `find_pivots`, неподтверждённый последний swing, чужие default-пороги |
| TradingView Auto Chart Patterns | Сначала целостность реальной формации, потом оценка; отдельно forming/confirmed/invalidated и breakout | Внутренний недоступный алгоритм TradingView, предварительные pivot в Robot |
| TradingView Auto Trendlines | Разделить базовый отрезок линии между опорами и дальнейшую проекцию до подтверждённого пробоя/последнего доступного бара; сохранять историю опор | Внешние 5/5, 25/25, ATR/длительность/3-bar breakout defaults, предпочтение самой длинной линии |
| pytrendline (https://github.com/ednunezg/pytrendline) | Проверка пересечения линии телом свечи, независимый учёт касаний и breakout, возможная группировка почти совпадающих локальных линий, если действительно нужна | Зависимость/дублирующий генератор: полный перебор `O(N^3)` неприемлем как default для 771 символа; сторонний scoring не определяет локальный эпизод |
| Freqtrade lookahead/recursive analysis; SciPy prominence | Проверки prefix-vs-future и устойчивости исходной истории; prominence измерять только если существующих raw pivots объективно не хватает | Новые обязательные зависимости, ML/картинная подгонка или неизвестные заранее swing points |

Ссылки на первичные материалы TradingView/LEAN/Freqtrade/SciPy находятся в §2; pytrendline — открытый внешний пример, не доказательство того, что его код или пороги подходят без адаптации. Не скачивать/копировать чужой исходный код, не устанавливать новые торговые библиотеки для S2b/S3.

### 5A.3. Существующие узлы, которые нужно использовать вместо нового механизма

- `geometry/trendline.py::fit_anchor_trendline` и `calculate_price` — единые линии/цены; `geometry/pair_metrics.py` и `geometry/envelope_metrics.py` — ширина, сходимость, пересечения; текущие расчёты ATR/касаний/серий и `geometry/envelope_metrics.py::evaluate_candle_containment()` — готовая диагностическая база.
- `geometry/envelope_metrics.py::evaluate_body_zone_breaches()` УЖЕ измеряет пересечения тел для двух сторон, но использует ATR-tolerance, пропускает бары без ATR и не эквивалентна новой строгой проверке; `wedge/integrity.py::evaluate_containment_violations()` УЖЕ обрабатывает нарушения Falling как soft penalty, а Rising в этом механизме не покрыт. Перед S3 проверить фактические интерфейсы и владельца каждого существующего результата.
- Локальные исследовательские `S1a/episode_ledger.py`, `S2a/boundary_pairs.py` и S0–S2a diagnostics лежат только в git-ignored `reports/research_scanner/diagnostics/` изолированного worktree; можно переиспользовать в read-only shadow, не считать опубликованным production API. Не создавать новый Geometry Engine, ATR-движок, scorer или полную систему ZigZag.
- `geometry/evaluation.py` сейчас понижает `CANONICAL` из-за ранней серии выходов, но изначально `EXPLORATORY` не подвергается симметричному структурному отклонению; `geometry/engine.py` ранжирует уже выбранные независимые линии. Структурная допустимость локальной пары должна предшествовать приоритету `CANONICAL` и любому ranking. Не чинить неверную фигуру переносом START, расширением графика или score.

### 5A.4. Строгая граница, окно проверки и точность

- FALLING — верхняя граница; RISING — нижняя. **На закрытых свечах внутри действующей формации тело не пересекает строгую линию:** `max(open, close) <= upper(t)` / `min(open, close) >= lower(t)` с учётом только доказанной точности хранения/ценовой сетки; старая процентная или ATR-tolerance НЕ разрешает реальное пересечение телом. Сохранять величину каждого пересечения и отдельно маркировать случаи, потенциально сравнимые с округлением. Минимальный наблюдённый шаг OHLC не считать официальным tick size Bybit.
- Небольшие одиночные выступы внешней тенью возможны; серийные, массовые или суммарно существенные — причина структурной нецелостности. Пока измерять число/долю, серии, максимум и накопленную глубину в цене/ATR/доле ширины; возле apex доля ширины неустойчива. Числовые пороги теней НЕ назначать до проверки размеченных положительных/отрицательных случаев отдельно на 1m и 5m.
- Отделять A: экстраполяцию до первого строгого анкера; B: обоснованный заякоренный участок; E: продолжение после последнего анкера до подтверждённого завершения либо `as_of`; C: участок только ПОСЛЕ независимо подтверждённого breakout/completion. Apex или последний анкер сами по себе НЕ подтверждают завершение; при отсутствии доказательства `completion_status=UNKNOWN` и E не исключать ради чистого результата. Всегда показывать FULL до исходного `as_of`, не прятать ранние пробои переносом START/common_start или поздние — произвольным обрезанием.
- Построимая линия не становится локальной из-за касаний чужих пивотов; оба якорных ряда и подтверждающие развороты обязаны относиться к одной последовательно развивающейся формации. Первые четыре разворота — ранняя гипотеза, не навсегда зафиксированные вторые анкеры; любой поздний анкер должен быть доступен на своём confirmation time и иметь доказанную непрерывную связь с предшествующим состоянием. HIGH+LOW на одной свече — `AMBIGUOUS`, unclosed/right-unconfirmed — недоступен.

### 5A.5. Укороченная последовательность выполнения (один зависимый шаг за раз)

**Следующий шаг: S2b, ТОЛЬКО shadow, пять контрольных кейсов.** Переиспользовать S2a `boundary_pairs.py`, S1a ledger и готовые функции кода, при необходимости добавить лишь минимальный read-only адаптер в отдельный ignored diagnostics-каталог. Для ALT/CRCL/CRV/DATA/0G сопоставить альтернативные локальные пары и первый `as_of_closed`, когда подтверждены ОБА вторых анкера; повторить проверку на этом историческом префиксе и последующих подтверждённых изменениях. Зафиксировать старую/новую опору, непрерывность общего эпизода, истинное сужение, подтверждения ВНУТРИ него, строгую границу A/B/E/FULL, неизвестный/доказанный breakout, точные нарушения и причины INVALID/UNKNOWN. CRCL L114 vs L123 — положительный/отрицательный контроль, CRV — развитие/завершение UNKNOWN, ALT и 0G — не форсировать клин, DATA — не понижать min_confirmations ради U80, SOL — MISSING_REPLAY_DATA. Не выбирать самую длинную линию, production winner или торговый сигнал.

**S2c — только при конкретном нерешённом блокере S2b.** Если существующие функции и пять кейсов дают проверяемое локальное правило, не создавать отдельный проект, каталог или новый исследовательский слой; переходить к подготовке узкого S3. Если нет — сформулировать ровно одну доказанную недостающую функцию/разметку и выполнить один bounded микрослайс. Отсутствующую пользовательскую разметку не подменять уверенностью по скриншоту.

**S3 — отдельное согласование перед реализацией.** Подготовить минимальный wedge-only structural gate до mode-priority/ranking, переиспользуя существующие линии и метрики; не создавать независимый параллельный hard/soft containment pipeline. Новый запрет пробоя строгой границы — проверка структурной корректности, а прежнее `DOCUMENTS/SCANNER_GEOMETRY_ATR_CONTAINMENT_DECISION.md` (accepted 2026-09-11) оставляет Falling ATR-based нарушения как soft penalty. Эти решения РАЗЛИЧАЮТСЯ: до production-изменения явно согласовать изменение контракта, место единственного gate, судьбу старых штрафов без двойного учёта и mirror Rising; не редактировать старое историческое решение так, будто оно никогда не действовало. Не менять Triangle, существующие Robot admission/size/STOP/TAKE/LIVE, PAPER state или уже frozen candidates. При UNKNOWN/INVALID не fallback к дальней линии и не отправлять новый клин в торговый контур.

**S4/S5 — только после подтверждения локальной формации и отдельного допуска:** определить исторический START и контекстные подтипы по предшествующему импульсу, не по одному порядку LOW/HIGH; early 4-confirmed-pivot candidate, refresh, внутриклиновую торговлю и кнопку «🤖 Робот» исследовать/согласовывать отдельно. G1 — отображение, не способ исправлять геометрию.

**Verification/rollout:** S2b — узкие as-of/prefix и five-case checks без полного сканирования; если S3 разрешён — сфокусированные wedge-only regression + неизменность non-wedge/Robot и shadow сравнение на замороженных 395 5m снимках, затем отдельно необходимые 1m и визуальные Telegram-кейсы. Точное пользовательское сопоставление ALT/CRV/DATA/0G по-прежнему отсутствует; не объявлять полную визуальную приемку. Рабочие процессы, основной `C:\BybitScanner`, VPS, Telegram listener, исследовательский сканер и PAPER DB не изменять без отдельного разрешения; локальный worktree может отставать от GitHub, не синхронизировать работающий код ради чтения плана.

### 5A.6. S2b завершён: подтверждённый объём и границы вывода (2026-09-20)

**Статус:** завершён только read-only shadow на пяти контролях; результаты находятся в git-ignored `reports/research_scanner/diagnostics/S2b_local_boundary_comparison_2026-09-20/` изолированного `scanner-g2a`, не входят в PR. Claude Code сообщил 6/6 targeted checks, неизменность 24 snapshot-файлов, отсутствие изменений production/процессов/торгового состояния. Эти результаты требуют сверки с локальными артефактами перед использованием как регрессионных фикстур; это не acceptance работающего сканера.

- **CRCL:** `U119→195 / L114→185` — `VALID_RESEARCH_PAIR` на своём историческом префиксе `as_of=198`: 9 HIGH/9 LOW подтверждений внутри эпизода, сужение 44.2%, строгая верхняя граница A/B/E/FULL без пересечений телами или тенями. Все исследованные пары от L123 не прошли. Варианты со вторыми анкерами 175/180 и 185/195 могли быть валидны *каждый на своём* более раннем `as_of` (178/183/188/198), но поздние пивоты нарушили прежние границы. Это свидетельствует о последовательности связанных исследовательских состояний с сохранёнными первыми опорами; **не** доказывает неизменность старой линии, сохранение прежнего frozen сигнала, подтверждённый пробой или прибыльность.
- **CRV:** заякоренные пары `U115→175 / L114→165` и `U124→159 / L122→165` чисты внутри B, но продолжение E содержит 4/2 пересечения телами строгой нижней границы (до 1.14/0.16 ATR соответственно). HIGH 194 — только кандидат события, затем нарушения с бара 195; независимого подтверждения завершения нет, `completion_status=UNKNOWN`. Не объявлять продление чистым, не обрезать E по последнему анкеру.
- **DATA:** `U80→146 / L85→135` сходится, но у строгой верхней линии нет дополнительного подтверждения сверх двух анкеров; `UNKNOWN` уже на собственном историческом префиксе. Поздняя U127 не восстанавливает пользовательскую гипотезу.
- **ALT:** на закрытых свечах исходного сигнала от L117 нет нисходящей пары, от L125 доступные варианты расширяются; сильные выходы локальных подтверждённых разворотов за оболочку исключают рассмотренные пары. HIGH 196 исходно не был подтверждён и не использовался. **0G:** проверенные пары нецелостны; совпадающие HIGH/LOW остаются `AMBIGUOUS`. **SOL:** `MISSING_REPLAY_DATA`.
- S2b использовал готовые S1a/S2a/проектные функции, добавил лишь read-only расчёт первого общего момента подтверждения двух вторых анкеров, последовательной классификации нового support/violation, сравнения со старой ATR-логикой и флаги округления. 18 из 31 сопоставимых пар дают отличающиеся количества нарушений в строгом и ATR-подходах. Новый `S2c` без конкретного доказанного блокера не нужен. Итог строго ограничен сохранёнными 5m-контролями: 1m, пользовательские alignment для ALT/CRV/DATA/0G и реальное runtime-поведение не подтверждены.

### 5A.7. S3: обязательный узкий прединтеграционный контракт (НЕ разрешение на изменение кода)

**Дальнейший шаг — один read-only S3-preflight/implementation SPEC, затем отдельное разрешение на реализацию**; не начинать production-патч, переключение работающего сканера, merge или Robot deployment из факта S2b PASS.

1. **Область:** wedge-only, первоначально проверенные 5m источники, без Triangle и изменения PAPER/LIVE admission, sizing, STOP/TAKE, existing frozen Robot signals или процессов. При отсутствии явного TF в доступном пути нельзя молча обойти gate для неподдерживаемого TF или выдать непроверенное поведение за 5m acceptance. Перед интеграцией проверить все call-sites и контракт `GeometryModel`, включая последствия `None`/INVALID/UNKNOWN для существующего сигнального/Robot потока.
2. **Источник as_of и raw:** фактический `analyzer/core.py` и вызовы `pivots.find_pivots` могут получать формирующуюся свечу; `geometry.engine.analyze_geometry` пока принимает раздельные filtered highs/lows, а не исследовательский confirmed ledger. Выделить существующий источник последней закрытой свечи; зафиксировать один точный путь передачи raw confirmed pivots/episode provenance без изменения неблизких паттернов. Проверка после generation не спасёт ALT, если локальная линия там вообще не создана; S3 должен честно ограничить scope или включить один минимальный wedge-only кандидатный путь. Не подменять as_of сдвигом графика или текущей свечой.
3. **Единственная точка допуска:** переиспользовать существующие линии/метрики и один wedge-owned structural verdict, доступный в цикле `geometry/engine.py::analyze_geometry` после обычной `validation.valid`, но **до** `rank_geometry` и `mode_priority`. `geometry/evaluation.py::evaluate_candidate_pair` может вычислять сырые метрики, но pattern-aware решение и владение STRICT/FLEXIBLE должно быть единым и явно названным (Wedge Layer либо одна доказанная адаптация, без второго конкурирующего hard gate). Проверить направление клина по обеим линиям до применения wedge-only gate; не менять Triangle и иные геометрические модели.
4. **Предмет проверки:** обе линии и все подтверждения принадлежат одному локальному эпизоду; оба вторых анкера подтверждены на исходном as_of; правильные наклоны/порядок/сужение; нет выходов структурных разворотов за оболочку; строгая граница FALLING-upper/RISING-lower не пересекается телами внутри наблюдаемой формации; отдельный учёт редких/массовых теней; A/B/E/FULL не прячут ранние нарушения или неподтверждённые late breaches. Пара с `UNKNOWN` или `AMBIGUOUS` не объявляется валидной и не заменяется дальней альтернативой. Нельзя приравнивать apex/последний анкер к confirmed breakout или отрезать E для искусственно чистого результата.
5. **Исторический ATR containment:** `DOCUMENTS/SCANNER_GEOMETRY_ATR_CONTAINMENT_DECISION.md` — принятое решение 2026-09-11 о Falling soft penalty, а `wedge/integrity.py` на проверенной ветке выставляет `CONTAINMENT_VIOLATION_EVALUATION_ENABLED = False`: до S3 необходимо проверить фактические call-sites и явно решить контракт между структурным отказом и отключённым legacy штрафом. Не переписывать историю как будто soft penalty действует сейчас. Расширение зеркальной строгой границы Rising требует явного согласования; старые 60/40 ATR зоны и новые A/B/E/FULL не должны выдавать два противоречащих допуска. Не переводить текущий ATR множитель в допуск пересечения тела и не делать двойной расчёт отказа/штрафа.
6. **Приёмка только в отдельной защищённой реализации:** test-only sample/check для CRCL L114 accept и L123 reject; ALT/0G reject без fallback; DATA UNKNOWN; CRV B-vs-E/completion UNKNOWN; replay `as_of` и prefix-vs-future, last forming bar, одновременно HIGH+LOW, корректность обеих ориентаций, отсутствие изменения Triangle/non-wedge, Robot signal/admission/ownership. После узких тестов — shadow на замороженных 395 5m сигналах с учётом зависимых наблюдений и выборочности; 1m — отдельно, без заявления текущей 1m приемки. Никаких live/Telegram изменений автоматически.

**Вывод для следующего исполнителя:** сначала представить конкретный S3 SPEC/дифф-границы, перечислить реальные существующие интерфейсы и отдельные изменения принятого ATR-контракта, выделить только требующие пользовательского решения последствия. Не объявлять предложенное расположение gate гарантированно достаточным до проверки as_of, raw provenance и паттерн-владения; не создавать новый PR/worktree/сканер/Geometry Engine.

### 5A.8. S3-PREFLIGHT: verified code path and safe first micro-slice (2026-09-20)

**Status:** read-only source inspection from Claude Code and GitHub branch; no production-code authorization or trading rollout. Local report: S3-PREFLIGHT; frozen five-control shadow evidence stays in git-ignored `reports/` and is not in GitHub. Repo HEAD at inspection: `c2fac39`.

**Actual data path:** `bybit_api.get_candles` → `analyzer/candles.py::load_candles` (no closed-candle trim) → `analyzer/core.py::find_pivots(df)` (returns only filtered) → `current_index=len(df)-1` → `wedge/analyzer.py::analyze_wedge` → `geometry/engine.py::analyze_geometry` (independent upper/lower candidate generation, evaluation, `validation.valid`, rank/mode priority) → `wedge/detector.py` (final pattern) → score/filter → notification / snapshot / Robot admission. The example ALT used a HIGH that needed the still-forming bar to be confirmed. A post-candidate `continue` cannot create the missing ALT L117 line; raw confirmed input and an episode-scoped wedge-only candidate source will be needed for any full fix.

**As-of safety contract:** at the eventual integration, derive a deterministic last-closed source index from explicit UTC source bar OPEN timestamps, actual timeframe duration and observed/request or saved signal time; for historical replay use the original stored decision time, NOT present wall-clock. Verify source timestamp units, candle order, gaps and OHLC integrity, and freeze one common as_of for pivot detection, geometry evaluation, and provenance. Do NOT simply compute raw pivots on full DF and filter `confirmed_at <= as_of`: first truncate/copy to closed candles before detection so future bars cannot influence historical extrema. Original `current_index`, source candle timestamp/cursor and Robot projection must not be silently rebased or changed as a side effect of shadow research.

**Actual legacy contract:** `wedge/integrity.py::CONTAINMENT_VIOLATION_EVALUATION_ENABLED=False` on inspected branch. Its existing Falling ATR soft-penalty calculation is present but disabled, and Rising is not covered by that legacy function. New structural strict-boundary rejection is a DIFFERENT policy and requires explicit agreement before turning on a production gate. Existing historic ATR decision retained as history; future addendum must state active behavior, strict vs quality ownership, no double counting, Rising applicability and unchanged Triangle. Unsupported TF must have an explicit plan, never a silent bypass or a claim that the 395 preserved 5m signals certify 1m.

**Correction of suggested S3-1:** doing six cross-cutting production-file edits and attaching `local_structure` to `analyzer/core.py` result/Robot snapshot is NOT behavior-neutral: serialized outputs and consumers may change even if selected winner is unchanged. Also adding a new field to the already selected GeometryModel cannot demonstrate all rejected alternative pairs. Shadow diagnostics must remain isolated from notification, signal snapshots, geometry cursor and Robot. The claim `Robot snapshot unchanged` can only be made after comparing full serialized payloads (excluding explicitly independent diagnostics), not assumed from score equality.

**Next single dependency, S3-1a (candidate raw API; no gate):** if implementation is authorized, make ONLY a behavior-preserving extraction in `pivots.py`: expose pure `detect_pivots` returning unfiltered HIGH/LOW with identical source-index semantics; preserve the exact signature, filtering, output types and legacy behavior of `find_pivots`. Read-only focused equality checks before/after on frozen snapshots (including input immutability, NaN/reset_index handling, tie-high/low, short/empty), plus existing relevant tests. Do NOT alter `analyzer/core.py`, `current_index`, scanner/Robot output, Telegram, START, ranking, thresholds, TF, legacy flag or gate in this slice. Publish only via the existing GitHub PR and required scoped verification. The running local scanner/worktree is not an implementation target: do not sync, edit source under a running process, or restart it. If the only available local verification would mutate its worktree, stop and report the exact proof gap; use GitHub-native safe checks instead of creating speculative infrastructure.

**Following dependent slices (not authorized together):** S3-1b as_of-confirmed inputs and provenance behind an opt-in independent diagnostic path, verified on historical prefixes without altering existing outputs; S3-1c wedge-only episode-scoped candidate generation and per-pair shadow verdict; then separate review/approval of fail-closed admission, Rising mirror, window semantics, serialisation/Robot implications and historical contract before any active S3 gate. No fallback to distant lines for INVALID/UNKNOWN. Neither “all five research cases reproduce” nor old `validation.valid` is sufficient for trading acceptance.

### 5A.9. S3-1a опубликован: raw-pivot API без подключения к сканеру

**Проверенный GitHub commit:** `2032819f24b62251555bb640f910b3e5b9f415ea` в существующей ветке PR #156. Изменены только `pivots.py` и новый `tests/test_pivots_detect_equivalence.py`; draft PR остаётся открытым. `pivots.detect_pivots(df, left=3, right=3)` возвращает raw HIGH/LOW на копии переданного кадра, без side-filter; legacy `find_pivots(df, left=3, right=3, min_change=0.003)` сохраняет прежний filter и наблюдаемое in-place приведение колонок. `detect_pivots` **НЕ** усекает незакрытые свечи и **НЕ** даёт provenance подтверждения: это отдельная обязанность вызывающего S3-1b пути. После `dropna/reset_index` индексы указывают на позиции очищенного кадра, поэтому историческую идентичность source timestamp нельзя молча принимать по номеру строки при неполных OHLC.

**Доказательства и ограничения:** исполнитель сообщил 9/9 read-only проверок на замороженном 395-snapshot manifest (0 расхождений legacy-output и состояния входного кадра) и 11/11 целевых тестов на публикуемых байтах. GitHub diff подтверждает два изменённых файла и выделение общей детекции. Для нового commit не обнаружено GitHub commit statuses; CI-success не утверждается. Запущенный исследовательский сканер и основной ПК/VPS не менялись по отчёту исполнителя; локальный `scanner-g2a` может сохранять старые `pivots.py` и документ и не должен синхронизироваться, пока под ним работает сканер.

**Ровно следующий микрослайс S3-1b:** read-only isolated shadow по сохранённым OHLC: на основе исходного времени решения, UTC open-time, реальной длительности source TF и явного closed-candle rule определить последний полностью закрытый бар, **усечь КОПИЮ кадра до** вызова опубликованной `detect_pivots` и построить raw confirmed ledger с EVENT/CONFIRMED_AT/source timestamps. Не предполагать UTC/ms/index при missing/duplicate/NaN source data; `UNKNOWN` или `MISSING_AS_OF` вместо wall-clock подстановки. Проверить исторический префикс vs appended future, consistency ALT HIGH196 absent at as_of198, внебиржевой режим на существующих сохранённых снимках. Сохранить результат только в новом git-ignored diagnostics-каталоге, не добавлять `local_structure` к `analyzer/core.py` результату, `GeometryModel`, Scanner/Telegram payload, Robot snapshot/cursor. Никаких production code/tests/docs, git checkout/pull/reset/commit/push и переключений процессов внутри S3-1b; опубликованный в GitHub модуль читать из точного commit или проверенной изолированной копии, а не импортировать старый локальный файл. В конце выдать минимальную доказанную границу следующего шага, не включать gate.

## 6. Acceptance / отклонённые короткие пути

Позитивные пользовательские картинки ALT, CRCL, CRV
не подменяют source-time верификацию и могут иметь
неподтверждённые на тот момент конечные points; отмечать
такое явно. Нельзя устранять ложные клины поднятием
QUALITY/MIN_SCORE, улучшением только chart_window,
переносом START, подгонкой линии к нарисованной картинке
или произвольной константой «максимум 2 часа».
Нельзя после находки wrong anchor разом переписывать
все geometry/pivots/Robot/G3P.

Required narrow tests на стадиях S1–S3:
- confirming pivot right-delay и immutable past-vs-full
  replay на 1m/5m и на соседних candle-cutoffs;
- HIGH→LOW и LOW→HIGH с разными prior impulse directions;
- raw pivot present / omitted filtered, same-side noise,
  tie HIGH+LOW same candle, unclosed bar, missing source OHLC;
- coherent shallow support and steep resistance как
  CRCL, rising correction как CRV; reject far anchor SOL,
  reject geometric false positive 0G на совпадающих
  сохранённых данных, а не на вручную придуманной картинке;
- не смешивать сохранённые pre/post signal pivot sets;
- существующие coherent wedges не пропадают исключительно
  из-за удаления CANONICAL-first или смены окна истории;
- baseline production candidate unchanged in shadow;
  wedge-only change doesn't alter non-wedge detectors,
  score/position ownership, Robot protection or closed trades.

### Owners / approvals

Ведение исследования: isolated local scanner snapshots + отдельный
черновой GitHub PR; не редактировать ignored research_scanner/ файлы
через GitHub, поскольку их там нет. Изменения repo-owned
source/tests/docs — GitHub-first после scoped diff/CI; опыт
должен быть воспроизводим на frozen snapshots. Работающий
исследовательский 771-symbol процесс сейчас НЕ заменён
этим планом — старые сигналы остаются экспериментальными.
Ни автоматическое присвоение subtype, ни его торговый
приоритет, ни снятие режима PAPER/разрешение LIVE этим
документом не утверждены.

Связанные локальные записи: DOCUMENTS/GEOMETRY_RESEARCH_2026-09-20.md
(G1/G2a/G2b0/G3P/G3R/G3C), DOCUMENTS/BACKLOG.md (приоритет
G2-P0) и DOCUMENTS/EXTERNAL_REFERENCE_REUSE_POLICY.md
(ADAPT/DEFER; без копирования внешнего proprietary source).
