# Robot v0.1 — Local Paper Run Result

**Дата фиксации:** 12.09.2026  
**Среда:** локальный PC, `C:\BybitScanner`  
**Режим:** Paper Runtime  
**Источник данных:** `paper_runtime.sqlite3`

## Фактически установлено

На момент проверки:

- `robot_runtime_state.mode = ROBOT_RUNNING`
- `robot_runtime_state.recovery_status = READY`
- Robot находился в рабочем состоянии.

В таблице `robot_candidates` обнаружены 2 кандидата:

| Symbol | Status | State revision |
|---|---|---:|
| AAVEUSDT | APPROVED | 0 |
| BLURUSDT | APPROVED | 0 |

В таблице `robot_trades`:

- записей: **0**

## Итог запуска

Robot фактически:

1. был запущен в состоянии `ROBOT_RUNNING`;
2. находился в состоянии `READY`;
3. получил и сохранил 2 Robot-кандидата;
4. оба кандидата получили статус `APPROVED`;
5. ни одной записи `robot_trade` создано не было;
6. факт открытия Robot-сделки в данном запуске **не установлен / отсутствует**.

## Важное ограничение

Другие таблицы Paper Runtime содержат значительную активность (`executions`, `paper_limit_orders`, `paper_limit_actions` и др.), однако без дополнительного анализа нельзя приписывать эту активность именно Robot. Поэтому она **не считается результатом Robot-торговли** в данном отчёте.

## Статус расследования

Причина, по которой `APPROVED`-кандидаты не перешли в `robot_trades`, **в рамках этого запуска не исследовалась**.

Следующее исследование не выполнялось, чтобы сохранить чистый результат наблюдения.

## Контрольная точка

**Robot запускался и работал.  
2 сигнала были APPROVED.  
0 Robot-сделок.**
